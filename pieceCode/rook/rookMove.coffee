@using zomChess

# Separate functions allows for a specific direction to be cancelled once it 
@var rookLoop1(currentPiece)
@var rookLoop2(currentPiece)
@var rookLoop3(currentPiece)
@var rookLoop4(currentPiece)